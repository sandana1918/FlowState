# Windows Setup

1. Install Docker Desktop and Node.js 20+.
2. Run `.\scripts\setup-windows.ps1`.
3. Start backend and frontend dev servers.
4. Configure the GitHub webhook to point at `/api/webhooks/github`.

